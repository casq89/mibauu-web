export type Category = {
  id?: string;
  name: string;
  description: string;
  enable: boolean;
};

export type CategoryDefaultState = {
  id?: string;
  name?: string;
  description?: string;
  enable?: boolean;
};
